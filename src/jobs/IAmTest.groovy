package jobs;
/*def myloop(...ary){
    ary.each { println it }
}

def test(permissions) {
    for (String single_permission : permissions) {
        println (single_permission)
    }
}

test(['Cédric', 'Guillaume', 'Jochen', 'Paul'])*/
